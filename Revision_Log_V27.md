# Revision_Log_V27
- **Mathematical Fix:** Addressed the unbounded logit summation vulnerability in $S_{raw}$. Replaced the raw addition with a local per-patch softmax $\tilde{P}^{(v_j)}(c)$ before Top-$k$ pooling. This mathematically ensures that patches with abnormally high L2-norms cannot arbitrarily dominate the aggregation.
- **Clarification of $\Phi_{calib}$:** Explicitly defined the dimensions for the lightweight projector ($\Phi_{calib}: \mathbb{R}^{D_{vision}} \rightarrow \mathbb{R}^{D_{llm}}$), confirming it maps to the latent dimension prior to $W_{vocab}$, drastically reducing the parameter space and clarifying its mechanism.
- **Narrative De-escalation:** Shifted the tone away from inventing a "grand theoretical paradigm" (toned down "Resonance" rhetoric) to focus purely on the concrete mechanics: token-local visual evidence and structural masking. Adjusted the title accordingly.
- **Protocol Enforcements:** Enforced the reviewer's strict execution guidelines: AGL is mandated as a primary column in Table 1; `BRA_no_VASM` is explicitly defined to penalize *all* tokens equally; `BRA_MeanPool` is rigidly defined to use the exact same projector and VASM as `AdaptiveTopK`; and the 3-part Failure Case (Figure 1) requirement is documented.
- **Retained Strengths:** Kept the praised VASM framework (BPE inheritance), the honest `BRA_zero`/`BRA_calib` split, and explicitly preserved the boundary strictly mapping to Image-LLMs without forcing video. All claims remain tied to the pre-registered pending experiments.