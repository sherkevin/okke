# Revision_Log_V16

- 将 `论文大纲_V8_标杆版.md` 冻结复制为新的正式最新版本 `论文大纲_V16.md`，明确切断 `V11-V15` 旧错误题眼链条对后续迭代的污染。
- 保留新的合法核心：`decode-time / logits-space intervention`、`token-local visual evidence`、`VASM` 的结构保护作用，以及 `BRA_zero / BRA_calib` 的公平边界。
- 明确放弃旧的标题级 framing：不再把 `Pooling Paradox` 作为摘要、引言或标题的中心命题。
- 后续 reviewer / scientist 自动迭代必须以 `V16` 为唯一正式基座，而不是继续在 `V15` 上滚动。
- 当前仍需未来评审与真实实验验证的重点：
  - `Phi / alignment` 是否足够稳
  - `VASM` 对 reasoning path 的必要性
  - `BRA_zero` 相比 `BRA_MeanPool` 的 dense grounding 优势
  - 视频主线是否值得保留为强主线
