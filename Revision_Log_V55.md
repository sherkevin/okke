# Revision_Log_V55
- **Terminology Fix (CRITICAL):** Completely eradicated the term "Test-Time Adaptation" (TTA) from the manuscript. Correctly reframed `BRA_calib` as an "Offline-Calibrated Plug-in Adapter" and a "Parameter-Efficient Fine-Tuning (PEFT)" module, aligning with rigorous mathematical definitions.
- **Math Clarification:** Added the explicit formulation $logit^{(v_j)}[c] = \Phi_{calib}(h_L^{(v_j)}) \cdot W_{vocab}^T$ in Section 3.1 to remove ambiguity regarding how the local evidence projects to the vocabulary dimension.
- **VASM Brittleness / English Limitation:** Added bolded disclaimers in Section 3.3 and Section 5 acknowledging the "English Dependency" and "OCR Paradox" (where non-dictionary/garbled strings default to $\gamma=0$, bypassing the intervention entirely).
- **Downgraded InfoNCE Claims:** Reframed the Graph and Semantic Vetos purely as "algorithmic stabilizers" rather than theoretical breakthroughs, promising to strip them if the naive baseline performs adequately.
- **Experimental Protocol Enhancements:** 
  - Added a `BRA_random` ablation (Random Spatial Control) to Chain A to isolate routing utility from arbitrary visual noise injection.
  - Added an OCR OOV Rate ablation to Chain B (plotting DocVQA performance vs. OOV rate) to track the inaction boundary.
  - Specified that Chain C heatmaps will isolate the *exact autoregressive subword step* (e.g., "ball" in "baseball").
  - Added Defense Line 6: "The EMA Kill-Switch Test" to plot the threshold trajectory over a >500 token chain, proving the EMA does not permanently flatline.
- **Retained Strengths:** Maintained the strict 2D Image-LLM scope, the `Base + 5k LoRA` baseline control, and the emphasis on the VASM Trigger Rate reporting.