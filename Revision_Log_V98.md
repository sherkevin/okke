# Revision_Log_V98
- **Removed Defensive Meta-Framing:** Stripped out phrases like "scientifically honest" and "executable contract" from the Abstract and Introduction to adopt a cleaner, more standard scientific tone per reviewer feedback.
- **Addressed the $O(M \times N_v)$ Latency Threat:** Clarified the "static late-fusion visual logit bias" architecture in Sec 3.2. Explicitly mapped out that `Phi_calib` is a one-time static projection post-prefill, limiting per-step overhead to $O(M \times N_v)$ scalar lookups. Split latency in Table 1 into *Projector Overhead* and *Routing Overhead*, and explicitly defined the Pareto frontier in Figure 1 with `tokens_per_second`.
- **Addressed VASM Semantic Fragility (Polysemy):** Acknowledged the static dictionary lookup flaw (e.g., "Apple" fruit vs company) in Sec 3.4 and Discussion. Introduced `TLRA_Oracle_VASM` (dynamic POS tagger) into Table 2 to mathematically bound the damage of this heuristic.
- **Addressed Logit Distortion ($\Delta_L$):** Added a mathematical clipping boundary ($\delta_{max}$) to the resonance penalty formula in Sec 3.3 to prevent aggressive distortion on spiky base distributions.
- **Addressed the "Identity Crisis" / Calib Fairness:** Enforced the strict requirement to compare against `Base + LoRA` (prefill adaptation) and added the `Global_Visual_State` baseline to Table 3 to prove that *local spatial patches* matter more than a global prompt injection.
- **Clarified Architecture Bounds:** Explicitly stated in Introduction and Sec 3.1 that the method only supports spatial-preserving architectures (like ViT+MLP LLaVA) and breaks on Q-Formers.