# 工程师 B 复制即跑命令（标杆V3）

> 目标：跑通负控制与风险链，不再为不成立的强 claim 烧算力。
> 对应文档：`论文大纲_标杆V3.md`、`实验方案_标杆V3.md`、`双工程师并行任务_标杆V3_20260322.md`

## 0. 先登录并进入项目

```bash
ssh -p 47559 root@connect.westc.seetacloud.com
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
mkdir -p logs/v3_engineer_b
```

## 1. `DocVQA` 先做 smoke，再做正式负控制

先确认 loader 不是空的。

```bash
CUDA_VISIBLE_DEVICES=3 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
python bra_eval_matrix.py \
  --model qwen3vl2b \
  --dataset docvqa \
  --n_samples 20 \
  --bra_method tlra_full \
  --output logs/v3_engineer_b/docvqa_smoke_tlra_full.json
' > logs/v3_engineer_b/docvqa_smoke_tlra_full.log 2>&1 &
```

如果 smoke 能读到样本，再跑正式负控制：

```bash
CUDA_VISIBLE_DEVICES=3 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
python bra_eval_matrix.py \
  --model qwen3vl2b \
  --dataset docvqa \
  --n_samples 200 \
  --bra_method tlra_full \
  --output logs/v3_engineer_b/docvqa_tlra_full.json ;
python bra_eval_matrix.py \
  --model qwen3vl2b \
  --dataset docvqa \
  --n_samples 200 \
  --bra_method tlra_no_vasm \
  --output logs/v3_engineer_b/docvqa_tlra_no_vasm.json
' > logs/v3_engineer_b/docvqa_main.log 2>&1 &
```

如果 smoke 仍是空结果，不要空转，直接收 diagnosis：

```bash
sed -n '1,200p' logs/v3_engineer_b/docvqa_smoke_tlra_full.log
```

## 2. `FREAK` 风险复核

当前代码里没有 `TLRA_RandomK` 可直接运行的入口，所以今晚只做可执行、可复现、最关键的 parity 复核。

```bash
CUDA_VISIBLE_DEVICES=2 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
python bra_eval_matrix.py \
  --model qwen3vl2b \
  --dataset freak \
  --n_samples 300 \
  --bra_method tlra_meanpool \
  --output logs/v3_engineer_b/freak_tlra_meanpool.json ;
python bra_eval_matrix.py \
  --model qwen3vl2b \
  --dataset freak \
  --n_samples 300 \
  --bra_method tlra_adaptivetopk \
  --output logs/v3_engineer_b/freak_tlra_adaptivetopk.json
' > logs/v3_engineer_b/freak_parity.log 2>&1 &
```

## 3. `VidHalluc` appendix-level 最小可读结果

先求有可读结果，再谈视频是否保留。

```bash
CUDA_VISIBLE_DEVICES=2 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
python bra_eval_matrix.py \
  --model qwen3vl2b \
  --dataset vidhalluc \
  --n_samples 80 \
  --bra_method tlra_full \
  --output logs/v3_engineer_b/vidhalluc_tlra_full.json ;
python bra_eval_matrix.py \
  --model qwen3vl2b \
  --dataset vidhalluc \
  --n_samples 80 \
  --bra_method tlra_adaptivetopk \
  --output logs/v3_engineer_b/vidhalluc_tlra_adaptivetopk.json
' > logs/v3_engineer_b/vidhalluc_main.log 2>&1 &
```

## 4. `Video-MME` 只做可评测性判断

这一步的目标不是刷结果，而是判断 loader 是否真正能消费样本。

```bash
CUDA_VISIBLE_DEVICES=3 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
python bra_eval_matrix.py \
  --model qwen3vl2b \
  --dataset video_mme \
  --n_samples 32 \
  --bra_method tlra_full \
  --output logs/v3_engineer_b/video_mme_tlra_full_smoke.json
' > logs/v3_engineer_b/video_mme_smoke.log 2>&1 &
```

如果仍是 `Loaded 0 samples` 或空结果，就不要继续加样本，直接回收诊断日志。

```bash
sed -n '1,220p' logs/v3_engineer_b/video_mme_smoke.log
```

## 5. 完成后统一检查

```bash
cd /root/autodl-tmp/BRA_Project
ls -lah logs/v3_engineer_b
```

## 6. 最低回收物

```bash
printf "%s\n" \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/docvqa_smoke_tlra_full.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/docvqa_tlra_full.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/docvqa_tlra_no_vasm.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/freak_tlra_meanpool.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/freak_tlra_adaptivetopk.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/vidhalluc_tlra_full.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/vidhalluc_tlra_adaptivetopk.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/video_mme_tlra_full_smoke.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/docvqa_smoke_tlra_full.log \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_b/video_mme_smoke.log
```

## 7. 结果解释规则

- `DocVQA` 的目标是证明 OCR concession 真实存在，不是证明方法在 OCR 上很强。
- `FREAK` 如果还是 `MeanPool >= AdaptiveTopK`，就直接判定当前版本不支持强 local-evidence superiority claim。
- `VidHalluc` 只要还空，就降为 appendix-only exploratory pilot。
- `Video-MME` 只要 loader 仍不可消费，就写成资源布局未达成正式 benchmark 条件。
