# 工程师 A 复制即跑命令（标杆V3）

> 目标：固化主线正证据。
> 对应文档：`论文大纲_标杆V3.md`、`实验方案_标杆V3.md`、`双工程师并行任务_标杆V3_20260322.md`

## 0. 先登录并进入项目

```bash
ssh -p 47559 root@connect.westc.seetacloud.com
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
mkdir -p logs/v3_engineer_a
```

## 1. 冻结 `TLRA_calib` 资产身份

先把现有候选权重列出来，并把结果保存成文本记录。

```bash
cd /root/autodl-tmp/BRA_Project
mkdir -p logs/v3_engineer_a
ls -lah models/V_matrix*.pt | tee logs/v3_engineer_a/phi_candidates.txt
python register_asset_audit.py > logs/v3_engineer_a/register_asset_audit.txt 2>&1
```

如果你已经确认正式 `Phi_calib` 路径，先执行：

```bash
export TLRA_PHI="/root/autodl-tmp/BRA_Project/models/REPLACE_WITH_OFFICIAL_PHI_CALIB.pt"
test -f "$TLRA_PHI" && echo "$TLRA_PHI"
```

如果 2 小时内仍无法冻结正式 `Phi_calib`，直接停止 `tlra_calib` 主表扩展，转去完成 `Chain A` 与 `Chain B` 的非 calib 主表。

## 2. `Stage 0` 复核

```bash
CUDA_VISIBLE_DEVICES=0 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
python tlra_semantic_validity_pilot.py \
  --model qwen3-vl-8b \
  --method tlra_zero \
  --n_samples 64 \
  --topk 10 \
  --candidate_window 50 \
  --output logs/v3_engineer_a/stage0_tlra_zero_8b.json
' > logs/v3_engineer_a/stage0_tlra_zero_8b.log 2>&1 &
```

如果 `TLRA_PHI` 已冻结，再补 `tlra_calib`：

```bash
CUDA_VISIBLE_DEVICES=0 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
export TLRA_PHI="/root/autodl-tmp/BRA_Project/models/REPLACE_WITH_OFFICIAL_PHI_CALIB.pt"
python tlra_semantic_validity_pilot.py \
  --model qwen3-vl-8b \
  --method tlra_calib \
  --projector_checkpoint "$TLRA_PHI" \
  --n_samples 64 \
  --topk 10 \
  --candidate_window 50 \
  --output logs/v3_engineer_a/stage0_tlra_calib_8b.json
' > logs/v3_engineer_a/stage0_tlra_calib_8b.log 2>&1 &
```

## 3. `Chain A` 正式主表

先跑不依赖 `Phi_calib` 的稳定主表。

```bash
CUDA_VISIBLE_DEVICES=1 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
python run_eval_pipeline.py --model qwen3-vl-8b --dataset pope  --method base      --mini_test 200 ;
python run_eval_pipeline.py --model qwen3-vl-8b --dataset pope  --method vcd       --mini_test 200 ;
python run_eval_pipeline.py --model qwen3-vl-8b --dataset pope  --method dola      --mini_test 200 ;
python run_eval_pipeline.py --model qwen3-vl-8b --dataset pope  --method tlra_zero --mini_test 200 ;
python run_eval_pipeline.py --model qwen3-vl-8b --dataset chair --method base      --mini_test 150 --chair_max_new_tokens 384 ;
python run_eval_pipeline.py --model qwen3-vl-8b --dataset chair --method vcd       --mini_test 150 --chair_max_new_tokens 384 ;
python run_eval_pipeline.py --model qwen3-vl-8b --dataset chair --method dola      --mini_test 150 --chair_max_new_tokens 384 ;
python run_eval_pipeline.py --model qwen3-vl-8b --dataset chair --method tlra_zero --mini_test 150 --chair_max_new_tokens 384
' > logs/v3_engineer_a/chainA_main.log 2>&1 &
```

如果 `TLRA_PHI` 已冻结，再追加 `tlra_calib`：

```bash
CUDA_VISIBLE_DEVICES=1 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
export TLRA_PHI="/root/autodl-tmp/BRA_Project/models/REPLACE_WITH_OFFICIAL_PHI_CALIB.pt"
python run_eval_pipeline.py --model qwen3-vl-8b --dataset pope  --method tlra_calib --mini_test 200 --projector_checkpoint "$TLRA_PHI" ;
python run_eval_pipeline.py --model qwen3-vl-8b --dataset chair --method tlra_calib --mini_test 150 --chair_max_new_tokens 384 --projector_checkpoint "$TLRA_PHI"
' > logs/v3_engineer_a/chainA_calib.log 2>&1 &
```

## 4. `Chain B` 正式化

先把当前最有价值的 `VASM` 证据做厚。

```bash
CUDA_VISIBLE_DEVICES=2 nohup bash -lc '
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
python bra_eval_matrix.py --model qwen3vl2b --dataset mmbench --n_samples 300 --bra_method tlra_full    --output logs/v3_engineer_a/mmbench_tlra_full.json ;
python bra_eval_matrix.py --model qwen3vl2b --dataset mmbench --n_samples 300 --bra_method tlra_no_vasm --output logs/v3_engineer_a/mmbench_tlra_no_vasm.json ;
python bra_eval_matrix.py --model qwen3vl2b --dataset mme     --n_samples 300 --bra_method tlra_full    --output logs/v3_engineer_a/mme_tlra_full.json ;
python bra_eval_matrix.py --model qwen3vl2b --dataset mme     --n_samples 300 --bra_method tlra_no_vasm --output logs/v3_engineer_a/mme_tlra_no_vasm.json ;
python bra_eval_matrix.py --model qwen3vl2b --dataset mmmu    --n_samples 200 --bra_method tlra_full    --output logs/v3_engineer_a/mmmu_tlra_full.json ;
python bra_eval_matrix.py --model qwen3vl2b --dataset mmmu    --n_samples 200 --bra_method tlra_no_vasm --output logs/v3_engineer_a/mmmu_tlra_no_vasm.json
' > logs/v3_engineer_a/chainB_main.log 2>&1 &
```

## 5. 导出结果文件清单

等作业完成后执行：

```bash
cd /root/autodl-tmp/BRA_Project
ls -lah logs/v3_engineer_a
```

## 6. 最低回收物

执行完后，至少回收这些文件路径：

```bash
printf "%s\n" \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/stage0_tlra_zero_8b.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/stage0_tlra_calib_8b.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/chainA_main.log \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/chainA_calib.log \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/mmbench_tlra_full.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/mmbench_tlra_no_vasm.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/mme_tlra_full.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/mme_tlra_no_vasm.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/mmmu_tlra_full.json \
  /root/autodl-tmp/BRA_Project/logs/v3_engineer_a/mmmu_tlra_no_vasm.json
```

## 7. 结果解释规则

- `TLRA_zero` 如果 patch-level overlap 仍低，就只保留为 appendix probe。
- `Chain A` 里如果 `CHAIR` 改善来自明显长度塌缩，不能写成正向胜利。
- `Chain B` 里如果 `tlra_full` 继续稳定优于 `tlra_no_vasm`，就把 `VASM` 作为正式主线必要模块。
