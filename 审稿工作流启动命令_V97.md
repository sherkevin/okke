# 审稿工作流启动命令（V97 起跑）

`V97` 已被设为新的 reviewer/scientist 讨论起点，它直接复制自 `论文大纲_标杆V5.md`。

## 本地启动

```bash
python paper_adversarial_workflow.py ^
  --workspace "d:\Shervin\OneDrive\Desktop\breaking" ^
  --source "论文大纲_标杆V5.md" ^
  --start-version 97 ^
  --max-version 117 ^
  --api-url "https://new.lemonapi.site/v1" ^
  --model "[L]gemini-3.1-pro-preview" ^
  --api-key "REPLACE_WITH_PRIMARY_KEY" ^
  --backup-api-key "REPLACE_WITH_BACKUP_KEY" ^
  --timeout-seconds 300
```

## 远程启动

```bash
ssh -p 47559 root@connect.westc.seetacloud.com
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
nohup python paper_adversarial_workflow.py \
  --workspace /root/autodl-tmp/BRA_Project \
  --source "论文大纲_标杆V5.md" \
  --start-version 97 \
  --max-version 117 \
  --api-url "https://new.lemonapi.site/v1" \
  --model "[L]gemini-3.1-pro-preview" \
  --api-key "REPLACE_WITH_PRIMARY_KEY" \
  --backup-api-key "REPLACE_WITH_BACKUP_KEY" \
  --timeout-seconds 300 > logs/workflow_v97.log 2>&1 &
```

## 本轮设计原则

- 新起点要吸收 `V86-V96` 的 observer 收口结果，但不继续过度负化叙事。
- reviewer 继续保持高自由度，用来发现新的稳定硬伤，而不是做 benchmark 验收。
- scientist 继续克制，只收编那些真正会提升方法定义准确性、实验公平性和可信度的修改。
- observer 继续负责判断哪些问题应升级为下一轮 benchmark 合同，哪些只是单轮 reviewer 的攻击性扩写。
