# Revision_Log_V25
- **Fixed the Critical `BRA_calib` Fairness Fallback Flaw:** Removed the scientifically invalid mandate to force LoRA fine-tuning on baselines like VCD/DoLa. The methodology now explicitly states that all baselines run on the standard frozen LLM, and `BRA_calib` runs on the *same* frozen LLM using its lightweight 5k-image projector. We transparently acknowledge this as a minor bounded asymmetry.
- **Refined VASM Limitations:** Explicitly addressed the reviewer's concern regarding out-of-dictionary visual entities (e.g., niche medical tools). Added the explicit boundary claim: "BRA is a highly effective, bounded intervention *if and only if* the target concept falls within the candidate window $\mathcal{C}_t$ and exists in the VASM dictionary."
- **Downgraded Video Temptation:** Removed unnecessary references to video, strictly anchoring the methodology and metrics (e.g., FREAK, DocVQA, OCR) to rigorous spatial and local evidence extraction, avoiding unproven temporal-local claims.
- **Solidified the Experimental Protocol Structure:** Reorganized Section 4 to map exactly to the reviewer's requested Tables 1, 2, and 3, defining explicit Columns, Rows, and Success Criteria for the three evidence chains. 
- **Retained Core Strengths:** Maintained the positive framing, AGL as a mandatory tracking metric, BPE dynamic inheritance logic, and the critical ablations (`BRA_MeanPool` and `BRA_RandomK`).