# 审稿工作流启动命令（V75 起跑）

`V75` 已被设为新的 reviewer/scientist 讨论起点，它直接复制自 `论文大纲_标杆V3.md`。

## 本地启动

```bash
python paper_adversarial_workflow.py ^
  --workspace "d:\Shervin\OneDrive\Desktop\breaking" ^
  --source "论文大纲_标杆V3.md" ^
  --start-version 75 ^
  --max-version 85 ^
  --api-url "https://new.lemonapi.site/v1" ^
  --model "[L]gemini-3.1-pro-preview" ^
  --api-key "REPLACE_WITH_PRIMARY_KEY" ^
  --backup-api-key "REPLACE_WITH_BACKUP_KEY" ^
  --timeout-seconds 300
```

## 远程启动

```bash
ssh -p 47559 root@connect.westc.seetacloud.com
cd /root/autodl-tmp/BRA_Project
export PATH="/root/miniconda3/bin:$PATH"
nohup python paper_adversarial_workflow.py \
  --workspace /root/autodl-tmp/BRA_Project \
  --source "论文大纲_标杆V3.md" \
  --start-version 75 \
  --max-version 85 \
  --api-url "https://new.lemonapi.site/v1" \
  --model "[L]gemini-3.1-pro-preview" \
  --api-key "REPLACE_WITH_PRIMARY_KEY" \
  --backup-api-key "REPLACE_WITH_BACKUP_KEY" \
  --timeout-seconds 300 > logs/workflow_v75.log 2>&1 &
```

## 本轮强口径

- `V75` 不是普通续写稿，而是新的 benchmark reset base。
- 从 `V75` 开始，自动讨论必须默认接受：
  - `TLRA_zero` 更像 viability probe
  - `VASM` 是当前最强主线资产
  - `DocVQA` 是 OCR concession 下的负控制
  - `AdaptiveTopK` 尚未证明强于 `MeanPool`
  - video 继续是 bounded secondary pilot
