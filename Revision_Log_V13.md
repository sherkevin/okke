# Revision_Log_V13
- **Fair Baseline Verification**: Addressed the "Zero-Shot Illusion" by explicitly bifurcating into $\text{BRA}_{zero}$ and $\text{BRA}_{calib}$. Mandated that $\text{BRA}_{zero}$ must be evaluated alongside $\text{BRA}_{calib}$ in all Defense Lines to prove the framework works purely at inference time without relying on VG data.
- **Memory Bandwidth Wall**: Shifted the computational argument from mere FLOPs to the true bottleneck: the $O(N_v)$ global VRAM *read* bandwidth ($\approx 800$ MB/step). Introduced fused Triton kernels and contiguous caching as the specific engineering solution to keep generation speed manageable. Added absolute latency (ms/token) tracking on A100 to the experimental plan.
- **Spatio-Temporal formulation**: Added the upper bound $k_{max}$ to the adaptive $k$ formula to prevent the pooling paradox from creeping back into ultra-long videos. Acknowledged the "bag-of-frames" limitation (Temporal Naivety) in the Discussion section.
- **Experimental Upgrades**: Added the Candidate Set ($M$) ablation, "% of BPE Continuations Penalized" tracking in Line 1, overlaid baseline curves for Video Histograms in Line 4, and $<16\times16$ object failure cases in Line 5.