# 3月25日前摘要冲刺任务清单

日期：`2026-03-22`

目标：

- 在摘要提交前，确保摘要所需数据与口径已经稳定
- 不追求“全部实验做完”，只追求“摘要里写得出的东西都站得住”

## A. 摘要必须依赖的稳定资产

1. `Table 2` 的稳定资产：
   - `MMBench`
   - `MME`
   - `DocVQA contract-ready`
   - `MMMU Hard` 修复后复核结论

2. `Table 1` 的首批 clean `8B` 结果：
   - 至少拿到 `POPE base`
   - 最好继续回收 `vcd / dola / tlra_zero`
   - 字段必须包含 `agl_stddev`、`peak_vram_gb`

3. `Base + LoRA` 的摘要口径：
   - 若正式 `50k` 数据入口到位，则尽快起正式训练
   - 若 `50k` 数据入口未到位，摘要中不得伪装成已有结果，只能写“matched-budget fairness baseline 已纳入正式合同，入口与预检已完成”

## B. 3月25日前最关键的三件事

1. 闭合 `GPU1` 上 `8B Table 1` 的第一批 clean 结果
2. 把 `Table 2` 现有稳定结果整理成摘要可引用口径
3. 明确 `Base + LoRA` 到底是：
   - 已正式启动
   - 还是仍被正式数据入口阻塞

## C. 3月25日前不应抢占主力资源的事项

- 视频线全部停止
- projector 未冻结前的 `FREAK` 正式化
- `Figure 1` 全量 sweep
- 扩展性边界测试（multilingual / CFG / MMHal 主表化等）

## D. 摘要写作约束

摘要允许写：

- 已稳定完成的 `Table 2` 方向结果
- 已稳定确认的工程事实与公平边界
- 已纳入正式合同但尚未完成的 fairness baseline，只能写成 protocol / contract，不得冒充结果

摘要不允许写：

- `FREAK` 正式结论
- `Base + LoRA` 未产出的数值
- `Table 1` 尚未回收的 clean `8B` 数值
- 任何视频正向结论
