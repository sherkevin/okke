# 已完成实验清单（V86）

日期：`2026-03-22`

目的：

- 单独记录截至当前已经完成、可引用、可诊断或仅可 provisional 使用的实验资产
- 便于后续与总实验任务文档、benchmark 主表占位、附录占位逐项对照

## A. 可直接作为当前论文资产使用

### 1. Stage 0 / `TLRA_zero`

状态：`completed`

结果位置：

- `experiment_logs/remote_mirror/v3_engineer_a/stage0_tlra_zero_8b.json`

当前用途：

- 支撑 `TLRA_zero` 更像 viability probe，而非主表核心方法
- 可直接填入 Stage 0 与 appendix 相关文字

### 2. Chain B / `MMBench`

状态：`completed`

结果位置：

- `experiment_logs/remote_mirror/v3_engineer_a/mmbench_tlra_full.json`
- `experiment_logs/remote_mirror/v3_engineer_a/mmbench_tlra_no_vasm.json`

当前用途：

- 支撑 `TLRA_full` vs `TLRA_no_VASM`
- 可作为 `Table 2` 的已完成资产之一

### 3. Chain B / `MME`

状态：`completed`

结果位置：

- `experiment_logs/remote_mirror/v3_engineer_a/mme_tlra_full.json`
- `experiment_logs/remote_mirror/v3_engineer_a/mme_tlra_no_vasm.json`

当前用途：

- 支撑 `VASM` 必要性相关主线资产
- 可作为 `Table 2` 的已完成资产之一

### 4. Chain B / `MMMU Hard`

状态：`completed_with_rerun_caveat`

结果来源：

- 工程师2 已用冻结 manifest `mmmu_hard_manifest_v3b.json` 完成正式对照

当前用途：

- 可以写入“MMMU Hard 已闭合”
- 旧的 `intervention_rate = 0.0` 结论已失效
- 但当前结果仍不支持强 `tlra_full > tlra_no_vasm` 叙事

## B. 已完成，但仅能作为 provisional / appendix / diagnostic

### 5. Chain C / `FREAK parity`

状态：`provisional`

说明：

- `MeanPool`、`AdaptiveTopK` 已跑完
- projector 身份未冻结，因此不能升格为正式主表结论
- 新 leakage / latency / continuation / provenance 字段链已打通

### 6. `DocVQA` negative control

状态：`contract_ready`

说明：

- `DocVQA` 正式补跑已完成并回收日志
- `bra_method` 不再塌缩
- `normalized_exact_match` 与 `anls` 已稳定存在
- `intervention_rate` 与 `sample_audits` 已保留

结论：

- 当前可以登记为 `Table 2` 的正式负控制资产

### 6A. `DocVQA` 导出协议修复 smoke

状态：`implemented_and_smoked`

结果位置：

- `/root/autodl-tmp/BRA_Project/logs/v3_contract/docvqa_tlra_full_export_smoke.json`
- `/root/autodl-tmp/BRA_Project/logs/v3_contract/docvqa_tlra_no_vasm_export_smoke.json`

当前用途：

- 证明正式导出协议已补齐：
  - `bra_method` 不再塌缩
  - `normalized_exact_match` 存在
  - `anls` 存在
- 说明 `DocVQA` 当前真正缺的是正式补跑结果，而不再是协议缺口

### 7. `VidHalluc`

状态：`completed_empty`

说明：

- 当前两次 run 都是 `Loaded 0 samples`
- 仅可作为 appendix 里“视频 pilot 未形成可读证据”的诊断结论

### 8. `Video-MME`

状态：`diagnostic_only`

说明：

- loader/index 已可审计
- 当前阻塞点是底层视频解码栈，不是数据入口
- 不进入主论文正式 benchmark

## C. 已完成的工程/协议资产

### 9. `TLRA_RandomK` 正式入口

状态：`implemented_and_smoked`

当前用途：

- `Table 3` 必要对照已具备真实代码入口

### 10. `MMMU Hard manifest` 入口

状态：`implemented_and_used`

当前用途：

- 已不再是单一 `Art` 子集伪评测

### 10A. `MMMU Hard` 导出与触发路径校验 smoke

状态：`implemented_and_smoked`

结果位置：

- `/root/autodl-tmp/BRA_Project/logs/v3_contract/mmmu_hard_tlra_full_export_smoke.json`

当前用途：

- 说明旧的 `intervention_rate = 0.0` 不能再直接当方法结论引用
- smoke 已表明在修复 `tlra_*` 实际触发 gating 后，`intervention_rate` 可非零落盘
- 同时 `visual_state_provenance`、`avg_vasm_time_ms`、`avg_routing_time_ms` 可稳定输出

### 10B. `MMMU Hard` 修复后正式复核

状态：`completed`

当前用途：

- 说明旧的“完全不触发”判断已经失效
- 新正式结果已可用于修正正文和实验方案口径

### 11. `Category Leakage Audit`

状态：`implemented_and_smoked`

当前用途：

- `FREAK` 可以带出 `seen/unseen` 与 `overlap_ratio`

### 12. `suffix-collapse / continuation audit`

状态：`implemented_and_smoked`

当前用途：

- 已能输出 `continuation_success_rate` 与 `suffix_collapse_failures`

### 13. `VASM / routing cost split`

状态：`implemented_and_smoked`

当前用途：

- 已能输出 `avg_vasm_time_ms`、`avg_routing_time_ms`、`latency_multiplier_vs_base`

### 14. `visual_state_provenance`

状态：`implemented_and_smoked`

当前用途：

- 已能在结果中写清 `h_L^{(v_j)}` 来源

### 14A. `Chain A` 主表字段协议修复 smoke

状态：`implemented_and_smoked`

结果位置：

- `/root/autodl-tmp/BRA_Project/logs/minitest/base_pope_20260322_102922.json`

当前用途：

- 证明 `Chain A` 输出协议已经能稳定落下：
  - `agl_stddev`
  - `peak_vram_gb`
- 后续第一份 clean `POPE/CHAIR` 正式结果必须核验是否带这两个字段

### 15. `Base + LoRA (Phi_calib data)` 脚手架

状态：`smoke_ready`

当前用途：

- reviewer 要求的 matched-budget baseline 已有训练入口
- 目前还不是正式实验结果

### 15A. `Base + LoRA` 正式配置包与预检

状态：`formal_configs_ready_but_data_blocked`

当前用途：

- `seed1/2/3` 正式配置包已固化
- `seed3_precheck` 已通过：
  - 日志正常
  - checkpoint 正常保存
  - `training_meta.json` 正常落盘
  - 内置与外部加载验收通过
- 当前唯一 blocker 不是训练入口，而是远端缺正式 `50k` 训练 JSONL

关键缺口：

- `/root/autodl-tmp/BRA_Project/train_data/phi_calib_matched_budget_50k.jsonl` 目前不存在

### 16. `VASM` 实现说明文档

状态：`completed`

文档：

- `VASM_IMPLEMENTATION_NOTE_20260322.md`

当前用途：

- 可直接服务论文方法论修订
- 已明确“无 online POS tagger in the loop”

## D. 当前仍未完成的主资产

- `Table 1` 的干净 `POPE` 多方法结果
- `Table 1` 的干净 `CHAIR` 多方法结果
- 带 `agl_stddev` 与 `peak_vram_gb` 的 clean `Table 1` 正式结果
- projector 身份冻结后的正式 `FREAK parity`
- matched-budget `Base + LoRA` 正式结果
- `Base + LoRA` 正式训练数据入口（50k JSONL）

## 总结判断

截至当前，最稳的已完成主线是：

1. `Stage 0`
2. `Chain B`
3. 工程协议与附录审计资产

最关键但仍未闭合的资产是：

1. `Table 1`
2. projector 身份冻结后的 `Table 3`
3. matched-budget `Base + LoRA` 正式结果
