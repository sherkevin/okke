# UniGround Entry Status (2026-03-22)

## Scope

This log records the first engineering closure for the `V6 / UniGround` entry work.
It is not a paper claim log. It records runner readiness, smoke results, and current blockers.

## What Was Completed

- Read and aligned to:
  - `论文大纲_标杆V6.md`
  - `实验方案_标杆V6.md`
  - `UNIVERSAL_PLUGIN_REDESIGN_NOTE_20260322.md`
  - `UNIVERSAL_PLUGIN_IMPLEMENTATION_SPLIT_20260322.md`
  - `认知洞察文档.md`
- Stopped the old `GPU1` Chain A process so the UniGround host-model work could use the assigned card.
- Added independent universal runtime:
  - `uniground_runtime.py`
- Added independent evaluation entry:
  - `run_uniground_eval.py`
- Added mechanism note:
  - `UNIGROUND_DECODETIME_MECHANISM_NOTE_20260322.md`

## New Entry Contract

- New output root: `logs/uniground_v6/`
- Supported method names:
  - `base`
  - `tlra_internal_zero`
  - `tlra_internal_calib`
  - `uniground`
  - `external_global_prior`
  - `uniground_no_gate`
  - `uniground_no_abstain`
  - `uniground_global_only`
  - `uniground_region_only`
- The new runner can reuse one loaded host model across multiple methods via `--methods ...`.

## Smoke Results Recovered

- `uniground_qwen3-vl-8b_pope_base_20260322_132210.json`
- `uniground_qwen3-vl-8b_pope_tlra_internal_zero_20260322_132424.json`
- `uniground_qwen3-vl-8b_pope_tlra_internal_calib_20260322_132452.json`

Observed facts from smoke:

- The independent runner loads `Qwen3-VL-8B`.
- Results are isolated under `logs/uniground_v6/`.
- `base`, `tlra_internal_zero`, and `tlra_internal_calib` all execute through the new entry.
- The output schema includes:
  - hallucination primary metric
  - ITL / TPOT
  - peak VRAM
  - universal-route audit slots (`intervention_coverage`, `prefix_ambiguity_rate`, `span_collapse_errors`, `abstention_rate`, `abort_trigger_rate`)

## Formal Mini-Run Results Recovered

- `uniground_qwen3-vl-8b_pope_base_20260322_140701.json`
- `uniground_qwen3-vl-8b_pope_tlra_internal_zero_20260322_140755.json`
- `uniground_qwen3-vl-8b_pope_tlra_internal_calib_20260322_140848.json`

Observed on `POPE random`, `mini_test=32`, `Qwen3-VL-8B`:

- `base`
  - `f1=0.9333`
  - `ITL/TPOT=22.17 ms/token`
  - `peak_vram=17.637 GB`
- `tlra_internal_zero`
  - `f1=0.9333`
  - `ITL/TPOT=21.32 ms/token`
  - `peak_vram=17.637 GB`
- `tlra_internal_calib`
  - `f1=0.9333`
  - `ITL/TPOT=21.28 ms/token`
  - `peak_vram=17.637 GB`

Notes:

- These three mini-runs were executed through `run_uniground_eval.py`, not the old internal-only runner.
- All three final JSONs were mirrored back into the local workspace under `experiment_logs/uniground_v6/`.
- For the internal methods, the universal-only audit fields are intentionally present but `null`; this preserves a stable result schema without falsely claiming universal-route behavior.

## External Global Prior Recovery

### Asset closure

- Downloaded `openai/clip-vit-large-patch14` locally via `hf-mirror.com`.
- Uploaded the required inference files to:
  - `/root/autodl-tmp/BRA_Project/models/clip-vit-large-patch14`
- Kept `run_uniground_eval.py` as the only `UniGround` entry and pointed the external encoder path to the recovered local directory on the remote host.

### Smoke result recovered

- `uniground_qwen3-vl-8b_pope_external_global_prior_20260322_143157.json`

Observed on `POPE random`, `mini_test=1`, `Qwen3-VL-8B`:

- `f1=1.0`
- `ITL/TPOT=76.16 ms/token`
- `peak_vram=17.636 GB`
- `intervention_coverage=1.0`
- `prefix_ambiguity_rate=0.4321`
- `span_collapse_errors=67.0`
- `abstention_rate=0.0`
- `abort_trigger_rate=0.0`

### Formal mini-run recovered

- `uniground_qwen3-vl-8b_pope_external_global_prior_20260322_143555.json`

Observed on `POPE random`, `mini_test=32`, `Qwen3-VL-8B`:

- `f1=0.9333`
- `ITL/TPOT=71.9 ms/token`
- `peak_vram=17.637 GB`
- `intervention_coverage=1.0`
- `prefix_ambiguity_rate=0.5574`
- `span_collapse_errors=103.4062`
- `abstention_rate=0.0`
- `abort_trigger_rate=0.0`
- `candidate_construction_ms=0.9772`
- `sidecar_scoring_ms=46.7546`
- `bridge_redistribution_ms=0.0546`
- `jitter_ms=17.3336`

Notes:

- This closes the `External_Global_Prior` baseline recovery for the first host model.
- The result JSON was mirrored back into the local workspace under `experiment_logs/uniground_v6/`.
- The host token is still generated by the original MLLM; the baseline only writes bounded decode-time bias on the current candidate window.

## Canonical First-Host Results

The canonical first-host formal mini-run set for `Qwen3-VL-8B` on `POPE random` is:

- `uniground_qwen3-vl-8b_pope_base_20260322_140701.json`
- `uniground_qwen3-vl-8b_pope_tlra_internal_zero_20260322_140755.json`
- `uniground_qwen3-vl-8b_pope_tlra_internal_calib_20260322_140848.json`
- `uniground_qwen3-vl-8b_pope_external_global_prior_20260322_143555.json`

All four are mirrored locally under `experiment_logs/uniground_v6/` and remotely under `logs/uniground_v6/`.
The internal-control JSONs were normalized to preserve the same required fields, including a `latency_split` object with `null` values where sidecar timing is not applicable.

## Current Blocking Gap

### Only active blocker: real `Psi_univ` checkpoint missing

No local or remote real `Psi_univ` checkpoint was found at verification time.

Impact:

- `uniground` cannot be run honestly yet.
- `uniground_no_gate` cannot be run honestly yet.
- `uniground_global_only` cannot be run honestly yet.
- I did not fabricate or silently replace `Psi_univ` with a heuristic scorer.

## Scientific Integrity Rules Followed

- Did not reuse host hidden states for the universal path.
- Did not route the new universal logic back into `run_eval_pipeline.py`.
- Did not claim `uniground` success without `Psi_univ`.
- Did not treat `tlra_internal_calib` as universal.
- Kept the universal route isolated from the old `phi_calib` result directory.

## Next Required Asset

To complete the first-host formal UniGround closure on `Qwen3-VL-8B` / `POPE random`, only one new asset is still required:

1. one real `Psi_univ` checkpoint from engineer 2.

Once it appears, immediately run through `run_uniground_eval.py` only:

1. `uniground`
2. `uniground_no_gate`
3. `uniground_global_only`

Those outputs must land in `logs/uniground_v6/`, be mirrored to `experiment_logs/uniground_v6/`, and then be handed to engineer 3 for validator processing before any positive interpretation is made.
