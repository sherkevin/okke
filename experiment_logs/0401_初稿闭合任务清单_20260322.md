# 4月1日前初稿闭合任务清单

日期：`2026-03-22`

目标：

- 在摘要提交之后，继续把主论文从“摘要可提交”推进到“初稿可闭合”

## A. 初稿闭合所需核心资产

1. `Table 1` 全部 clean `8B POPE/CHAIR`
2. `Table 2` 全部稳定口径写入正文
3. matched-budget `Base + LoRA` 正式结果
4. projector 冻结后的 `FREAK parity`
5. `Figure 1` 最基本 sweep

## B. 4月1日前按优先级的主任务

1. 收完整 `Table 1`
2. 跑完并验收 `Base + LoRA`
3. projector 冻结后完成 `FREAK` 正式化
4. 收齐 `Figure 1` 所需最基本 latency / `M` sweep

## C. 可以推迟到初稿之后的内容

- 视频线扩展
- 更大规模的 multilingual / OOD 审计
- 更复杂的 continuous baseline 扩展
- 更完整的 appendix 美化与扩展图

## D. 判断初稿是否可以闭合的标准

满足以下至少四项，可视为初稿基本闭合：

1. `Table 1` 完整
2. `Table 2` 完整
3. `Base + LoRA` 有正式结果
4. `Table 3` 从 provisional 升级
5. `Figure 1` 有最基本可成图数据
