# 标杆V4 收口修改清单

日期：`2026-03-22`

适用对象：

- 当前 benchmark：`论文大纲_标杆V4.md`
- 当前实验方案：`实验方案_标杆V4.md`
- 旁观者收敛依据：`experiment_logs/V86_V96_旁观者收敛结论_20260322.md`

目的：

- 不是生成 `标杆V5`
- 而是为后续精修 `标杆V4` 提供一份“只收口、不扩张”的修改清单

---

## 一、必须吸收的修改

### 1. 明确 `Base + LoRA` 为正式必选公平基线

修改位置：

- `Abstract`
- `1. Introduction`
- `3.1 Fairness Boundary`
- `4.2 / 4.3 / 4.4` 对应表格说明
- `4.7` 主表占位

修改动作：

- 将 `Base + LoRA (Phi_calib data / matched budget)` 升级为正式必选基线。
- 明确它不是附录或 optional sanity check，而是判断 `TLRA_calib` 是否具有独立方法价值的关键门槛。
- 在表格占位中为其预留行，但只放在与 `TLRA_calib` 公平相关的区域，不强行塞进所有 zero-shot 对照叙事。

为什么必须改：

- 这是 `V86-V96` 最稳定、最高价值的 reviewer 共识。
- 不写进去，`TLRA_calib` 的公平性始终站不住。

当前是否具备工程前提：

- `Base + LoRA` 脚手架已 smoke-ready，可先写进 benchmark 合同。

---

### 2. 把 `VASM` 的实现现实写实，但不要过度负化

修改位置：

- `3.4 Structure Preservation via Automated VASM`
- `5. Discussion and Limitations`
- `Appendix A / C` 占位说明

修改动作：

- 明确写成：
  - `offline precomputed vocab lookup`
  - `BPE continuation inheritance`
  - `no online POS tagger in the loop`
- 把 polysemy、subword collision、suffix-collapse 保留为 limitation / appendix 审计，而不是标题级主叙事。
- 保留 OCR concession，但不要把 `VASM` 升级成“fatal heuristic”的摘要级自我描述。

为什么必须改：

- reviewer 反复质疑在线 POS / 在线 parser 的真实性。
- 现在工程实现已经给出明确答案，这应被吸收到 benchmark。

当前是否具备工程前提：

- `VASM_IMPLEMENTATION_NOTE_20260322.md` 已存在。

---

### 3. 明确 `visual state provenance` 是方法定义的一部分

修改位置：

- `3.2 Bounded Candidate Filtering and Adaptive Top-k Resonance`
- `Appendix A / D` 占位说明

修改动作：

- 在方法定义中明确 `h_L^{(v_j)}` 不是抽象占位符，必须写出来源层、来源模块、是否 prefill 缓存。
- 用“需要在正式结果中写入 provenance 字段”来约束实验输出，而不是仅在实现里默许。
- 维持当前 benchmark 的克制表述，不直接把正文重写成 `mid-layer / final-layer` 的宏大表示学习论文。

为什么必须改：

- 这已经被多轮 reviewer 反复提升为方法定义本身。

当前是否具备工程前提：

- `visual_state_provenance` 已进入结果字段链路。

---

### 4. 把 `Top-M / Hijacking` 从 limitation 升级为正式量化资产

修改位置：

- `3.5 Methodological Boundaries`
- `4.5 Efficiency and Failure-Boundary Audits`
- `Figure 1` 及 appendix failure placeholders

修改动作：

- 明确写出：
  - `Top-M ceiling`
  - `out-of-candidate / hijacking` 需要量化
- 在图表占位中加入：
  - hijacking 统计图或 CDF
  - 对 `M in {10, 50, 100}` 的敏感性

为什么必须改：

- 这是当前论文最有价值的 failure-boundary 资产之一。

当前是否具备工程前提：

- 代码链路已开始具备相关字段与审计结构。

---

### 5. 在实验方案中把“已完成/未闭合/仅 provisional”状态显式刷新

修改位置：

- `实验方案_标杆V4.md`

修改动作：

- 依据 `experiment_logs/已完成实验清单_V86_20260322.md` 更新实验状态：
  - `Stage 0`：completed
  - `MMBench/MME`：completed
  - `MMMU Hard`：completed_with_caveat
  - `DocVQA`：completed_but_not_contract_ready
  - `FREAK`：provisional
  - `VidHalluc` / `Video-MME`：diagnostic_only
- 明确 `Table 1`、`Table 2`、`Table 3` 当前各自还缺什么。

为什么必须改：

- benchmark 与实验现实必须同步，否则工程和写作会继续脱节。

---

## 二、建议吸收，但先放在附录/实验方案层

### 6. 加强 `AGL StdDev`、`peak_vram`、latency split 的合同位置

修改位置：

- `4.2 Evidence Chain A`
- `4.5 Efficiency and Failure-Boundary Audits`
- `4.7 Table 1`

修改动作：

- 明确这些字段不是“有则更好”，而是 `Table 1` 完整闭合的必要组成。
- 增加一行说明：`AGL StdDev` 用于识别“通过输出长度收缩伪造收益”的风险。
- 成本拆分保留到 appendix 或实验方案，不必在主摘要里扩写。

状态：

- 可先写入合同，但要等待工程侧 clean 输出闭合。

---

### 7. 把 `Category Leakage / suffix-collapse / continuation audit` 升格为附录正式资产

修改位置：

- `Appendix B`
- `Appendix C`

修改动作：

- 将原先“计划”改为“正式附录位”。
- 明确附录字段：
  - `seen/unseen split`
  - `overlap_ratio`
  - `per_category_summary`
  - `continuation_success_rate`
  - `suffix_collapse_failures`

状态：

- 代码与 smoke 已具备，可在 benchmark 中升格为“应当填充”的附录项。

---

## 三、暂缓吸收，不进入当前 benchmark 主线

### 8. `mid-layer / layer sweep`

处理原则：

- 保留为高价值研究问题
- 但暂不重写成当前 benchmark 的正文主轴

原因：

- 当前工程现实还不足以支撑它成为新 benchmark 核心合同。

---

### 9. 更复杂的 continuous baseline

处理原则：

- 承认需要更公平
- 但暂不让 benchmark 围绕 `Continuous_Gated_XAttn` 膨胀

原因：

- 这类扩展容易把 benchmark 再次拉大发散。

---

### 10. MMHal / Distractor / multilingual audit

处理原则：

- 作为边界测试储备
- 可以进“候选附加审计列表”
- 不进入当前正文主合同

原因：

- 有价值，但不应反客为主把论文改写成“不可能性研究”。

---

## 四、明确不吸收的内容

### 11. 不把 reviewer 的攻击性自我定义搬进 benchmark

不要写进 benchmark 的措辞：

- `static late-fusion visual logit bias`
- `bag-of-words noun booster`
- `fatal heuristic`

原因：

- 这些措辞有诊断价值，但不适合作为 benchmark 自我定义。

---

### 12. 不把大规模扩展线一次性并入主合同

当前不并入的方向：

- CFG
- MMHal 主表化
- Distractor Prompt 主表化
- multilingual 主表化
- contrastive-calib-no-vasm
- vocabulary density sweep
- high-resolution `N_v` scaling

原因：

- 这些会让 benchmark 从收口重新变成发散。

---

## 五、建议的实际执行顺序

### 第一批：现在就可以改文档

1. `Base + LoRA` 升为正式公平基线
2. `VASM` 实现现实写实
3. `visual state provenance` 写入方法定义
4. `Top-M / Hijacking` 升级为正式量化资产
5. `实验方案_标杆V4.md` 的实验状态刷新

### 第二批：等工程主资产闭合后再改

1. `Table 1` 的 clean 状态与字段说明
2. 合规版 `DocVQA` 正式负控制说明
3. `MMMU Hard intervention_rate` 的最终解释
4. projector 冻结后的 `FREAK` 正式身份

### 第三批：再决定是否需要新 benchmark

触发条件至少满足两项：

1. `Table 1` clean 闭合
2. 合规版 `DocVQA` 完成
3. `MMMU Hard` 解释稳定
4. `FREAK` 从 provisional 升级

---

## 六、最终原则

这份清单的核心不是“把 `V86-V96` 的所有 reviewer 意见吸进去”，而是：

- 只吸收那些被多轮 reviewer 重复击中、且与当前工程现实相匹配的修改项
- 明确拒绝把攻击性 reviewer 语言直接变成 benchmark 自我定义
- 让 `标杆V4` 变得更硬、更准、更可执行，但不重新发散
